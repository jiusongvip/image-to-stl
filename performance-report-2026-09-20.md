# 性能复测报告 — www.image-2-stl.com

**测量时间**：2026-09-20 14:55–14:58 CST
**线上版本**：`0d75ced`（工作树干净，本地 = 远程）
**测量工具**：PageSpeed Insights API（移动 4 次 / 桌面 2 次） + 本地 Lighthouse 13.4.1（智能体浏览）
**测量前提**：每次请求 URL 均带随机 `?cb=` 参数，`fetchTime` 已核对互不相同（确认不是缓存命中）

---

## 一、结论速览

| 项目 | 移动端 | 桌面端 | 判定 |
|---|---|---|---|
| **Performance** | **97 / 83–84**（见 §三） | **99–100** | ⚠️ 移动端双峰，非代码问题 |
| Accessibility | 100 | **100**（修复前 96） | ✅ 对比度缺陷已修并上线 |
| Best Practices | 100 | 100 | ✅ |
| SEO | 100 | 100 | ✅ |
| **CLS** | **0.000**（6/6 次） | **0.000** | ✅ |
| Agentic Browsing | **100** | — | ✅ |

**一句话**：上一轮修的关键 CSS 内联**确实生效且已部署**（首绘几何、CLS、桌面端全部达标）；
移动端本次出现 83–84 与 97 的双峰，经逐帧排查确认**不是布局缺陷**，而是首绘提交时刻的抖动（详见 §三）。
复测中发现的桌面端无障碍 96 分（9 处文字对比度不足）**已修复并上线**（详见 §七）。

---

## 二、实测数字（移动端，4 次）

| # | 报告 FCP | 报告 LCP | 报告 SI | TBT | CLS | 实测 FCP | 实测 LCP | 首绘帧 |
|---|---|---|---|---|---|---|---|---|
| m-1 | 1712 | **4108** | **4010** | 69 | 0 | 2328 | 2328 | 2625 ms |
| m-2 | 1721 | **4146** | **3993** | 136 | 0 | 2314 | 2314 | 2625 ms |
| m-3 | 1722 | **4142** | **3964** | 102 | 0 | 2293 | 2293 | 2625 ms |
| m-4 | 1709 | 2251 | 2492 | 83 | 0 | **1289** | **1289** | **1500 ms** |

> 报告 FCP 稳定在 1709–1722 ms（极差 13 ms），但**实测 FCP 从 1289 摆到 2328**（极差 1039 ms）。
> 这是本站已知的「报告值 ≠ 实测值」现象，**判读分数前必须同时看两套值**。

## 三、移动端 83–84 分的归因：不是代码回归

三次落在 83–84 的测量，数字上**精确复现了修复前的特征**（LCP ≈ 4108/4146/4142、SI ≈ 4010/3993/3964、
`elementRenderDelay` = 2019/2044/2065）。这个巧合度足以让人误判成回归，所以做了三层验证：

**1）LCP 元素的 `boundingRect` 完全相同 —— 布局是对的**

```
慢轮 m-1： LCP 元素 boundingRect = { width: 378, height: 915, top: 90, left: 17 }
快轮 m-4： LCP 元素 boundingRect = { width: 378, height: 915, top: 90, left: 17 }
```

修复前的缺陷特征正是「首绘时 hero 图 `width:800px`」——现在是 **378px**，与定型值一致。
**布局层面没有回归。**

**2）解码胶片逐帧比对 —— 渲染结果一模一样**

| 帧时刻 | 375 | 750 | 1125 | 1500 | 1875 | 2250 | 2625 | 3000 |
|---|---|---|---|---|---|---|---|---|
| m-1（慢）JPEG | 4.1K | 4.1K | 4.1K | 4.1K | 4.1K | 4.1K | **20.5K** | 20.5K |
| m-4（快）JPEG | 1.2K | 1.2K | 1.2K | **20.5K** | 20.5K | 20.5K | 20.5K | 20.5K |

两者**终帧视觉完全一致**（hero 图尺寸正确、标题字体正确、内联样式生效），
差别只是「第几帧开始有内容」：慢轮 2625 ms、快轮 1500 ms。

**3）`observedLoad` 几乎相同 —— 网络不是瓶颈**

慢轮 `observedLoad` = 547 ms，快轮 = 431 ms，差 116 ms；
但首绘差 **1039 ms**。**网络早就完成了，页面迟迟不画。**

**判定**：这是 4× CPU 节流下合成器提交首绘的调度抖动，与页面代码无关。
`elementRenderDelay` ≡ 实测 LCP − 网络耗时，它随首绘时刻同步摆动（1073 ↔ 2019+），
不是「元素画错了在等修正」——因为 `boundingRect` 证明元素一开始就是对的。

**处置建议**：不改代码；引用移动端分数时以 97 那一档为准，并按本站纪律在报告里注明双峰现象。

## 四、桌面端（2 次，仲裁者）

| # | Perf | FCP | LCP | SI | TBT | CLS | `elementRenderDelay` |
|---|---|---|---|---|---|---|---|
| d-1 | **100** | 459 | 582 | 468 | 37 | 0 | 203 ms |
| d-2 | **99** | 464 | 582 | 537 | 86 | 0 | — |

桌面端缺口小、指标稳定，**Perf 99–100 / LCP 0.58 s / CLS 0，与修复后基线一致**。

## 五、LCP 四段分解（移动端）

| 轮次 | TTFB | loadDelay | loadDur | **elemRenderDelay** | 合计 |
|---|---|---|---|---|---|
| m-1 | 3 | 176 | 130 | **2019** | 2328 |
| m-2 | 3 | 157 | 110 | **2044** | 2314 |
| m-3 | 17 | 138 | 73 | **2065** | 2293 |
| m-4 | 4 | 159 | 53 | **1073** | 1289 |
| 桌面 d-1 | 3 | 81 | 107 | **203** | 394 |

前三段（TTFB + 加载延迟 + 加载耗时）在四轮间基本恒定（合计 214–309 ms），
**全部方差落在最后一段**。这与 §三 的结论一致：瓶颈是首绘提交时刻，不是资源获取。

## 六、智能体浏览（本地 Lighthouse 13.4.1，mobile）

**分类得分：agentic-browsing = 100 / 100**

| 审计 | 结果 | 说明 |
|---|---|---|
| `agent-accessibility-tree` | ✅ 1.0 | 无障碍树结构完整，全部子审计通过 |
| `llms-txt` | ✅ 1.0 | `llms.txt` 符合规范 |
| `cumulative-layout-shift` | ✅ 1.0 | 0.02（远低于 0.1 阈值） |
| `webmcp-registered-tools` | ➖ n/a | 未注册 WebMCP 工具（非必需） |
| `webmcp-form-coverage` | ➖ n/a | 建议为表单加 WebMCP 注解（增强项） |
| `webmcp-schema-validity` | ➖ n/a | 无 WebMCP schema 需要校验 |

三项 WebMCP 为「不适用」不扣分。**这是增强 AI 智能体可操作性的机会项，不是缺陷。**

## 七、优化机会清单（按优先级）

### ❌ P1 — 桌面端文字对比度不足（**已修复并上线**）

`Accessibility` 桌面 96 分，唯一扣分项 `color-contrast`，**9 个节点不合格**，两类：

| 元素 | 前景 | 背景 | 实测对比度 | 要求 |
|---|---|---|---|---|
| 工具面板按钮（`text-surface-500` on `bg-surface-800`） | `#64748b` | `#1e293b` | **3.07** | 4.5 |
| 表单标签 `label.text-xs.text-surface-500`（on `#020617`） | `#64748b` | `#020617` | **4.23** | 4.5 |

复现选择器：`div.rounded-tool > div.flex > button.flex-1`、`div.rounded-tool > div.space-y-1.5 > div.flex > label.text-xs`

**成因**：这些 12px 文字用了**单一颜色类**，但背景是随主题切换的
（`bg-surface-100 dark:bg-surface-800`）—— 浅色下 `#64748b` 尚可，深色下只有 3.07:1。
文字 12px + normal 字重，**不享受大字的 3:1 豁免**。

**修复（提交 `cdb88f8`，已上线）**：

| 场景 | 改动 | 对比度 |
|---|---|---|
| 深色 未选中标签/按钮 | `#64748b` → `#94a3b8` | 3.07 → **5.71** |
| 深色 滑块标签 | `#64748b` → `#94a3b8` | 4.23 → **7.87** |
| 浅色 未选中标签/按钮 | `#64748b` → `#475569` | 4.34 → **6.92** |

> 浅色侧 `#64748b` on `surface-100` = **4.34:1**，虽然当时没被 Lighthouse 点名
> （那些元素在 412px 视口下不渲染），但同样不达标，一并修正。

**A/B 验证（同口径 1350px 真实桌面视口）**：

| 版本 | Accessibility | contrast score | 失败节点 |
|---|---|---|---|
| 修复前（线上 `0d75ced`） | 96 | 0 | **9** |
| 修复后（本地构建） | **100** | 1 | **0** |

⚠️ **验证过程本身踩了一个坑，值得记录**：首次对照时「修复前」也报 100 分 / 0 失败。
原因是 `--form-factor=desktop` 若不同时覆盖 `--screenEmulation.width`，
**实际仍以 412px 渲染**，而那三个工具面板在该宽度下不显示 —— 失败元素根本没进 DOM。
**「对照组也通过」= 测量装置失效，不是修复有效。** 把宽度显式设为 1350px 后，
对照组立刻复现 9 个失败节点，验证才成立。

**部署后线上复测**：移动端与桌面端 **Accessibility 均 100 / 0 失败**；
移动 Perf 98 / LCP 1.8 s，桌面 Perf 98–100 / LCP 0.5 s。
内联关键 CSS 体积 15,053 → 14,953 B。

### ✅ P2 — 图片过大（**已修复并上线，含两轮**）

**第一轮（提交 `feb15ea`）**：`section#creations` 的 9 张图全部是 1024×1024，而桌面网格
实际只渲染 **414px** 宽，每张超标约 2.5 倍。`image-delivery-insight` 估计浪费 201 KiB。

修复：新增 `scripts/optimize-gallery-images.mjs`，重编码为 **512px**（lanczos3 / WebP q80），
输出到 `public/images/optimized/`，markup 的 `src` 与 `width`/`height` 同步更新（1024 → 512）。

**第二轮（提交 `feb15ea` 之后）**：第一轮只核对了首页。做**全站 15 页**审计时发现
**5 个详情页的 hero 图仍是 1024px 原图**（`svg-to-stl` / `heightmap-editor` /
`cookie-cutter` / `lithophane-maker` / `photo-to-3d`）。

这里差点做错：我原本打算沿用 512px。但**在 Chrome 里实测渲染宽度**（没有靠 class 推断）
得到的是 **766px**，不是 414px —— 因为详情页 hero 在 `max-w-3xl` 容器里，比首页网格宽。
**512px 会被放大 1.5 倍、肉眼可见发虚。** 于是把脚本改造成**双尺寸组**：

| 组 | 目标宽 | 用途 | 实测渲染宽 | 张数 |
|---|---|---|---|---|
| `gallery` | 512px | 首页网格（`aspect-square`）| 414px | 9 |
| `hero` | 860px | 详情页 hero（`max-w-3xl`）| **766px** | 5 |

860 = 766 × 1.12，留出 DPR 与亚像素余量，同时避免 1024 的浪费。

| | 前 | 后 |
|---|---|---|
| 首页 9 张 | 699,718 B | **248,554 B**（−64.5%，省 441 KiB）|
| 详情页 5 张 | 429,688 B | **279,508 B**（−35.0%，省 147 KiB）|
| **合计 14 张** | **1,129,406 B** | **528,062 B**（**−53.2%**）|
| `image-delivery-insight` | 0.5 / 201 KiB 浪费 | **1 / 0 浪费** |
| 移动端总传输 | — | **341 KiB** |

**刻意未动**：`/images/hero-workspace.webp`（首页 LCP 元素，800×446，仅 **15 KiB**，已是最优）
—— grep 曾把它列进「未优化原图」，但它尺寸与实际显示一致，改它有害无益。

脚本幂等（已 ≤ 目标宽直接跳过，不会二次降质），且**不自动改写 markup** ——
构建脚本静默改源码会让人无法分辨改了什么。

线上核验：9 张图 HTTP 200 / `image/webp` / 字节数与本地构建逐字节一致。

### ⚠️ P3 — 第三方脚本（不可完全控，部分可在 Cloudflare 面板关）

- `legacy-javascript-insight` 11 KiB — 全部来自 `static.cloudflareinsights.com/beacon.min.js`
  （Cloudflare Web Analytics）。**不在 origin HTML 里，是 CF 边缘注入的。**
- `cache-insight` 4 KiB — 同为 `beacon.min.js`（4.2 KiB）+ `email-decode.min.js`（0.4 KiB，
  CF Email Obfuscation 注入）。
- `unused-javascript` 72 KiB（移动端）— 主要是 GTM + GA。

**修法**：前两项只能在 **Cloudflare 控制台**关闭 Web Analytics / Email Obfuscation。
GTM 属业务必需，不建议删 —— 且实测桌面端偶发的 TBT 222 ms **全部来自 GTM/gtag 长任务**
（174 + 165 ms），是当前最大的剩余单点。

> 图片优化后，移动端传输量前 10 名里**已无画廊图片**（懒加载 + 已降采样），
> 剩余主要是 **GTM 172 KiB** 与字体（约 100 KiB）。

---

## 八、与修复后基线的对照

| 指标 | 修复前 `86c9e65` | 修复后基线 | **本次复测** | 判定 |
|---|---|---|---|---|
| 移动 Perf | 82–88 | 96–98 | 97（慢轮 83–84 属抖动） | ✅ 持平 |
| 移动 LCP | **4.1 s** | 2.25–2.48 s | **2.25 s** | ✅ 持平 |
| 移动 SI | **4.0 s** | 1.67–2.61 s | **2.49 s** | ✅ 持平 |
| 移动 CLS | 0.072 | 0.000 | **0.000** | ✅ 持平 |
| 桌面 Perf | 99–100 | 99–100 | **99–100** | ✅ 持平 |
| 桌面 LCP | 0.48–0.60 s | 0.48–0.60 s | **0.58 s** | ✅ 持平 |
| LCP 首绘宽度 | **800px（错误）** | 378px | **378px** | ✅ 已修 |

**关键 CSS 内联修复的收益在本轮完整保持。** 唯一的例外是 3 次移动端读数复现了修复前的
*数值特征*，但几何与胶片证明渲染结果正确 —— 这是测量抖动，不是回归。

---

## 九、测量可信度声明

1. **移动端报告值与实测值严重脱节**：报告 FCP 恒定 1709–1722 ms，实测 1289–2328 ms。
   按本站纪律，缺口 >500 ms 的轮次不作为性能结论依据 → **83–84 分那三轮的 LCP/SI 数值不予采信**。
2. **`timeToFirstByte` 恒定 601 ms**，而 `observedLoad` 仅 431–547 ms、实测首绘可早至 1289 ms
   —— TTFB 是独立口径，不代表本页真实 RTT。
3. **桌面端缺口小且稳定**（报告 582 vs 实测 394），因此**桌面端仍是仲裁者**。
4. **无 CrUX 字段数据**（页面与源站均「无」）→ 所有数字均为实验室数据，不代表真实用户分布。

---

*报告生成：2026-09-20 · 工具：PSI API + Lighthouse 13.4.1*

---

## 十、第十轮：字体回退面内联修复（提交 `6e33c58`）后复测

### 本轮修的是什么

**根因不是尺寸数值，而是回退字体根本没进入首绘。**

`scripts/inject-critical-css.mjs` 一直把**所有** `@font-face` 排除在内联关键 CSS 之外，
理由是「首屏用到的字面已在 `<head>` 里 preload」。这对 **webfont** 成立，
对 **metric-override 回退面** 不成立 —— 因为内联块里仍然含有 Tailwind 的 reset：

```css
font-family: Geist, "Geist Fallback", system-ui, sans-serif;
```

它**引用了**回退族名，而定义它的 `@font-face` 只存在于延迟加载的完整样式表里。
**被引用但未声明的字体族会被浏览器静默跳过**，于是首绘落在 `system-ui`，
Geist 到达后重新排版 → **hero 段落 7 行变 6 行**，CLS 0.0126–0.0208。

**所以调 `size-adjust` 永远不可能修好这个问题** —— 调好的那个面在交换前压根没被用到。

修法：把**无下载成本**的面（`src: local(...)`，仅 519 B 声明）内联进关键 CSS，
有下载成本的面保持延迟。

### 同时修正的 size-adjust（浏览器实测自校准，非文件推算）

| 字面 | 旧值 | 新值 |
|---|---|---|
| Geist Fallback w400 | 105.74% | **103.98%** |
| Geist Fallback w700 | 111.88% | **109.07%** |
| Geist Mono Fallback | 125.78% | 125.78%（不变） |

`src: local(...)` 的面**先解析到本机字体、再应用 size-adjust**，
所以比例应是 `target / (那个本机字体)` 而**不是** `target / system-ui` ——
用错分母是约 9% 的静默误差。

### 本地探针（4× CPU 节流 + 慢速 4G）

| | 修复前 | 修复后 |
|---|---|---|
| hero 段落行数 | **7 行 → 6 行** | **6 行 → 6 行** |
| 移位元素高度 | `182→156`（丢一行） | `149→149`（不变，仅 top 微移） |
| CLS | **0.0126（8/8 轮）** | **0.0000（5/6 轮）**；唯一例外 0.0045，且发生在字体 7461 ms 才到达时（通常 ~950 ms） |

### 线上复测（PSI，`?cb=` 随机参数，6 次独立测量）

| 轮次 | Perf | LCP(报告) | LCP(实测) | FCP | SI | TBT | **CLS** | TTFB |
|---|---|---|---|---|---|---|---|---|
| 移动 1 | 94 | 2.25 s | 2.33 s | 1.67 s | 3.97 s | 146 ms | **0.0000** | 601 ms |
| 移动 2 | 97 | 2.10 s | 1.33 s | 1.67 s | 2.79 s | 105 ms | **0.0000** | 601 ms |
| 移动 3 | 97 | 2.25 s | 1.35 s | 1.67 s | 2.58 s | 100 ms | **0.0000** | 601 ms |
| 桌面 1 | **100** | 0.49 s | 0.32 s | 0.49 s | 0.49 s | 37 ms | **0.0000** | 161 ms |
| 桌面 2 | **100** | 0.47 s | 0.28 s | 0.46 s | 0.46 s | 15 ms | **0.0000** | 161 ms |
| 桌面 3 | **100** | 0.50 s | 0.34 s | 0.46 s | 0.47 s | 19 ms | **0.0000** | 162 ms |

**六次测量 `layout-shifts` 审计的 `items` 全部为空（0 条移位记录）。**
字体交换重排不是「变小」，是**消失**。

| 类目 | 移动 | 桌面 |
|---|---|---|
| Performance | 94 / 97 / 97 | **100 / 100 / 100** |
| Accessibility | 100 | 100 |
| Best Practices | 100 | 100 |
| SEO | 100 | 100 |

### 优化机会：三项归零

| 审计 | 结果 |
|---|---|
| `render-blocking-insight` | score=1，**items=0**，节约 0 ms |
| `image-delivery-insight` | score=1，**items=0** |
| `font-display-insight` | score=1，**items=0** |

### 智能体浏览（本地 Lighthouse 13.4.1，agentic-browsing）

**满分 3/3（类目得分 1）**

| 审计项 | 权重 | 结果 |
|---|---|---|
| `agent-accessibility-tree` | 1 | ✅ All audits passed |
| `cumulative-layout-shift` | 1 | ✅ 0.007 |
| `llms-txt` | 1 | ✅ 通过 |
| `webmcp-*` / `ard-schema` | 0 | N/A（无表单，不计分） |

### LCP 分解：四段对账

| 轮次 | TTFB | 发现延迟 | 传输 | **渲染延迟** | 四段之和 | 实测 LCP | 本站长任务 |
|---|---|---|---|---|---|---|---|
| 移动 1 | 4 ms | 180 ms | 114 ms | 2033 ms | 2330 ms | 2330 ms | **0 ms** |
| 移动 2 | 4 ms | 102 ms | 47 ms | 1177 ms | 1331 ms | 1331 ms | **55 ms** |
| 移动 3 | 5 ms | 151 ms | 56 ms | 1142 ms | 1354 ms | 1354 ms | **59 ms** |

**四段之和与实测 LCP 逐轮精确相等**，无未归因时间 → 不存在结构性阻塞。

渲染延迟 1142–2033 ms **无法**由本站 0–59 ms 的长任务解释；
同期第三方（GTM 200–246 ms）+ 主线程总工作量仅约 486 ms。
这是 **Lighthouse 移动端 4× CPU 节流下图片解码/合成/绘制的固有开销**，
即本项目此前已确认的「测量装置上限」，**继续改代码无收益**。

### 新增自检（负向对照已验证）

`inject-critical-css.mjs` 现在断言：**内联块中出现的每一个 `*Fallback` 字体族，
都必须有内联的 `@font-face` 声明**。

负向对照实测：把三个面从内联块删掉后，脚本
**`exit 1` 并逐条点名** ——
`内联块引用了字体族 "Geist Fallback" 却没有对应 @font-face（首绘会回退到 system-ui 并产生重排）`，
两个族名都报出。**断言有牙齿，不是自欺。**
