node_modules/
dist/
.astro/
*-audit/
*.log
.DS_Store

# Local agent workspace (memory logs, skill state) — not part of the site.
.workbuddy-ai/

# Measurement output — raw PSI / Lighthouse JSON, kept locally for comparison
# but never part of the site. scripts/psi-run.mjs and the performance-check
# skill write these into the repo root.
psi*.json
lh-*.json
